# Receipt integration restoration — September 12, 2026

The Hermes Desktop full seven-team attempt produced one eligible report. Its scoped producer POST received HTTP 401, and the receipt journal stopped that immutable run ID. A credential-free HEAD probe of /coach/weekly-report/receipt returned 404 at 2026-09-13T03:13:28Z; the reviewed receipt handler rejects that method with 405 before checking credentials. Current main 71221d4 contains the legacy ingest-token handler and no receipt routes. These observations are consistent with the receipt handler being absent from the deployed route; they do not identify the deployed Worker version or certify its registry configuration.

This merge integrates the existing fix/receipt-legacy-evidence history into current main. It restores scoped producer receipt ingestion/lookup, isolated operator controls, immutable storage and audit, complete-coverage publication rules, coverage display, and existing operator tooling. It preserves current main's team-directory behavior, Rep features, dependency pins, and retired cloud timeline collection. The existing single historical sample exception remains restricted to its exact identity; it does not authorize new partial reports.

Validation: Worker and web TypeScript checks pass. Worker suite: 898 tests plus six team-link tests. Receipt operator: 29 tests. Web: 525 tests using NODE_OPTIONS=--no-experimental-webstorage on Node 26 so jsdom owns localStorage. Default Node 26 first produced nine unrelated workshop localStorage initialization failures; no workshop code was changed. Receipt tests execute the existing migration in isolated embedded Postgres.

No production deployment, live database changes, secret changes, or report replay was performed. Cloudflare deployment metadata could not be read because this session has no configured Cloudflare API token. The repository requires the designated production operator to deploy.

Before rollout, verify the already documented production receipt schema and scoped registry. The unchanged migration source is historical: deployment notes record its server-assigned migration history as 20260909022501, different from its source filename. Do not blindly reapply or roll back the migration. Deploy the merged Worker through the established production path, preserving current bindings and scheduled behavior. Verify recognized receipt routes and scoped producer authentication with read-only lookups before any authorized report retry. Preserve stopped receipt journals and exact report/run/team/hash identity; use the existing reconciliation process. Stored is not published, and release metadata is not proof of the actual Coaching view.

Read-only production verification subsequently confirmed both existing migrations: report_receipt_controls at 20260909022501 and preserve_legacy_quote_variants at 20260909145808. All five coach_receipt functions are SECURITY INVOKER, deny EXECUTE to anon/authenticated, and permit service_role. No migration or privilege change is required for restoration. Exact deployed Worker identity remains unverified.

Publishing is currently blocked: the local Git CLI has no authenticated GitHub login; the connected GitHub integration rejects Git-data writes with HTTP403 (Resource not accessible by integration). Cloudflare CLI lacks a configured API token. The tested local merge is retained; no PR or production deployment is claimed.
