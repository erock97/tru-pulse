import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
const dir=path.join(root,'web/public/workshops');
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const css=`body{margin:0;background:#f2f0e9;color:#171d22;font:17px/1.65 'DM Sans',sans-serif}main{max-width:960px;margin:auto;padding:50px 28px}h1,h2,h3{font-family:Manrope,sans-serif;line-height:1.15}h1{font-size:48px;font-weight:500;letter-spacing:-.04em}h2{margin-top:42px}header{padding:32px;background:#20292c;color:#f2f0e9}article{padding:24px 0;border-bottom:1px solid #bac1bd;break-inside:avoid}a{color:#234c69}header a{color:#bdd1ed}button{padding:12px 20px;background:#171d22;color:#f2f0e9;border:0;border-radius:8px;cursor:pointer;font:inherit}.meta{color:#51635c}.cue{padding:14px;background:#e5e7df}blockquote{margin:18px 0;padding-left:20px;border-left:3px solid #758b7b}.writing{min-height:90px;border-bottom:1px solid #adbab3}nav{display:flex;gap:20px;flex-wrap:wrap;margin:24px 0}@media print{button,nav{display:none}main{padding:0}body{font-size:11pt}h1{font-size:28pt}a{color:inherit}.writing{min-height:75px}}`;
fs.writeFileSync(path.join(dir,'guide.css'),css);
fs.writeFileSync(path.join(dir,'guide.js'),`document.querySelector('[data-print]')?.addEventListener('click',()=>window.print());`);
let fonts=fs.readFileSync(path.join(root,'web/src/workshops/fonts.css'),'utf8');
fs.writeFileSync(path.join(dir,'fonts.css'),fonts);
const shell=(title,body)=>`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)}</title><link rel="stylesheet" href="fonts.css"><link rel="stylesheet" href="guide.css"><script src="guide.js" defer></script></head><body><main>${body}</main></body></html>`;
for(let day=1;day<=4;day++){
 const data=JSON.parse(fs.readFileSync(path.join(dir,`day${day}.json`),'utf8'));
 const minutes=data.slides.reduce((n,s)=>n+s.time,0);
 const refs=day===3?'<li><a href="https://www.zillow.com/premier-agent/touring-agreement/">Zillow touring agreement</a> — current product explanation. Confirm the brokerage-approved form and local requirements.</li>':day===4?'<li><a href="https://www.zillow.com/homeloans/zillow-home-loans-faqs/">Zillow Home Loans FAQ</a></li><li><a href="https://www.zillow.com/preferred/zillow-home-loans/">ZHL agent tools</a></li><li><a href="https://www.consumerfinance.gov/owning-a-home/compare/">CFPB: comparing loan offers</a></li>':'';
 const header=`<header><p>TRU Rep · Zillow Preferred · Day ${day}</p><h1>${esc(data.title)}</h1><p>${minutes} minutes including practice · ${data.slides.length} screens</p></header>`;
 const cards=(data.cases||[]).map(c=>`<article><h3>${esc(c.name)}</h3><blockquote>${esc(c.quote)}</blockquote><p>${esc(c.goal)}</p></article>`).join('');
 const fields=data.slides.flatMap(s=>[...s.body.matchAll(/<label class="field">([^<]+)<textarea/g)].map(m=>m[1]));
 const common=`<h2>Practice after the workshop</h2><p>Within 24 hours, explain the lesson without notes and practice with a partner. In three days, try another buyer case. In seven days, review an actual example with your coach. These are suggested intervals, not scheduled reminders.</p>`;
 const guide=`${header}<nav><button data-print>Print facilitator guide</button><a href="day${day}-resources.html">Agent worksheet</a><a href="/#/workshop/${day}">Open presentation</a></nav><h2>Before you begin</h2><p>Keep this guide on a second screen or print it. Project the presentation window. Arrange pairs or trios and have paper available. Ask everyone to answer before revealing an example. After feedback, ask the agent to retry the specific part that needs work.</p><p>The timer supports short practice rounds. For longer rounds, use your room clock. Practice notes save on each device; they are not sent to the facilitator. Use hand votes or partner discussion for group responses.</p>${day===1?'<p>Use the training records only. All five existing record exercises remain in the lesson. In learner mode, the record check must pass before continuing. Presenter mode allows free navigation. Exercise edits survive moving between slides during the same lesson; leaving or reloading resets the exercise. Saved written workshop notes are separate.</p>':''}<h2>What to listen for</h2><p>${day===1?'The agent can explain the event behind the stage, save and verify the change, write a factual note, and schedule the next action.':day===2?'The agent uses LEAD: introduces themselves, offers a showing early, asks permission and follows the buyer’s answer, then summarizes and confirms the plan.':day===3?'The agent learns specific preferences and timing, handles financing preparation in context, and agrees on a useful next action.': 'The agent connects the introduction to the buyer’s concern, explains a relevant benefit without promising an outcome, asks permission, and confirms the next action and time.'}</p><h2>Facilitator notes</h2>${data.slides.map((s,i)=>`<article><p class="meta">${String(i+1).padStart(2,'0')} / ${data.slides.length} · ${esc(s.chapter)} · ${s.time} min</p><h3>${esc(s.title)}</h3><p>${esc(s.notes)}</p><p class="cue"><strong>Ask the room:</strong> ${esc(s.cue)}</p></article>`).join('')}${cards?'<h2>Buyer practice cards</h2>'+cards:''}${common}<h2>References</h2><p>Based on Eric’s supplied Day ${day} presentation${day<4?' and TRU sales doctrine':''}. Reference review: September 6, 2026.</p><ul>${refs}<li>The original source presentation is preserved. The redesigned workshop is a separate edition.</li></ul><h2>Training records</h2><p>${day===4?'This is a practice workshop. It does not award a certification or change existing certification scores.':'Existing course quizzes and certification records continue through the normal Rep course flow. The presentation link above is presenter mode, not a certification attempt.'}</p>`;
 const resource=`${header}<nav><button data-print>Print agent worksheet</button><a href="day${day}-guide.html">Facilitator guide</a></nav><h2>Practice notes</h2><p>Use fictional details for exercises. Write first, then compare with a partner. Keep one specific correction and retry it aloud.</p>${fields.map(f=>`<article><h3>${f}</h3><div class="writing"></div></article>`).join('')}${cards?'<h2>Buyer practice cards</h2>'+cards:''}<h2>Partner feedback</h2><p>What did you hear that helped the buyer? What should the agent try differently?</p><div class="writing"></div>${common}<h2>References</h2><ul>${refs||'<li>Your coach and the approved team workflow.</li>'}</ul>`;
 fs.writeFileSync(path.join(dir,`day${day}-guide.html`),shell(`Day ${day} facilitator guide`,guide));
 fs.writeFileSync(path.join(dir,`day${day}-resources.html`),shell(`Day ${day} agent worksheet`,resource));
}
console.log('Generated four facilitator guides and four agent worksheets.');
